import { render, screen } from "@testing-library/react";
import App from './App';

describe("App Component", () => {
    test("App compoent should render", () => {
        // render(<App />);
        // const appEl = screen.getByTestId('app');
        // expect(appEl).toBeInTheDocument();
    });
});